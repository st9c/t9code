#include <stdio.h>
#include <cs50.h>

int main(void)
{
  string s = ["Person A", "Person B", "Person C etc..."];
  string input = get_string("Type the Name of a Person:  ");
  for(int i = 0; i < s; i++)
  {
    if(input == s[])
    {
      printf("Person is Found.);
    }
    else
    {
      printf("Person is not found.);
  }
}
